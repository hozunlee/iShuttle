# [BE_001] 백엔드 스캐폴딩

> 상태: DONE
> 완료: 2026-03-23
> 담당: BE
> 생성: 2026-03-22

## 목표

FastAPI 앱 + 6단계 파이프라인 stub이 실행되고, `/api/analyze` 호출 시 job_id를 반환하는 상태

## 완료 기준 (Done Criteria)

- [ ] `uv run uvicorn main:app --reload --port 8000` 실행 성공
- [ ] `POST /api/analyze` → `{ job_id, status: "processing" }` 반환
- [ ] `GET /api/status/{job_id}` → `{ progress, step }` 반환
- [ ] `GET /api/results/{job_id}` → RallyReport JSON 반환 (mock 데이터)
- [ ] `WS /ws/progress/{job_id}` 연결 후 진행률 push
- [ ] 파이프라인 6단계 stub 순차 실행 (각 200ms sleep으로 시뮬레이션)

## 입력 (Input)

- CONTEXT.md (API 계약, 파이프라인 순서)
- backend/SPEC.md

## 출력 (Output)

- backend/main.py
- backend/api/analyze.py, results.py, feedback.py
- backend/pipeline/__init__.py + 6개 모듈 stub
- backend/pyproject.toml

## 의존성

- 선행: 없음
- 후행: FE_001이 이 API를 사용

## 메모 / 이슈

- Spec.md의 모델명 "claude-sonnet-4-20250514" → CONTEXT.md 기준 "claude-sonnet-4-6"으로 통일
- pipeline stub은 asyncio.sleep으로 진행률 시뮬레이션
