# [BE_007] 랠리 신고 엔드포인트 & 로그 저장

> 상태: TODO
> 담당: BE
> 생성: 2026-03-25

## 목표

`POST /api/report` 엔드포인트를 구현하여 사용자 신고 데이터를 `output/reports.jsonl` 에 저장한다.

## 완료 기준 (Done Criteria)

- [ ] `POST /api/report` 200 반환
- [ ] `output/reports.jsonl` 에 신고 데이터 append
- [ ] 저장 포맷: `{"rally_id", "job_id", "reason", "comment", "ts"}` JSON Lines

## 저장 포맷

```json
{"rally_id": 3, "job_id": "abc-123", "reason": "non_game", "comment": "준비 운동 구간", "ts": "2026-03-25T12:00:00Z"}
```

reason 값: `"non_game"` | `"boundary_error"` | `"score_error"` | `"other"`

## 구현

```python
# api/report.py
@router.post("/report")
async def submit_report(body: ReportRequest):
    with open("output/reports.jsonl", "a", encoding="utf-8") as f:
        f.write(json.dumps({...}, ensure_ascii=False) + "\n")
    return {"ok": True}
```

## 의존성

- 선행 필요: 없음
- 후행 영향: FE_007, 추후 모델 파인튜닝 데이터 소스
